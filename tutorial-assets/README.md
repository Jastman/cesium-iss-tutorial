# Tutorial screenshots

Drop screenshots here as we work through each part. The `TUTORIAL.md` write-up
will reference each file by the exact names below. Keep PNGs, ~1600 px wide, no
personal tokens visible in any frame.

## Checklist

### Part 1 — Scaffold
- [ ] `part-1-ion-token-page.png` — `ion.cesium.com/tokens` page. **Blur or crop the actual token value.**
- [ ] `part-1-vite-running.png` — Terminal showing `npm run dev` output with the local URL.

### Part 2 — Hello globe
- [ ] `part-2-globe.png` — Default blue Cesium globe loaded in browser, tilted home view, no entities.

### Part 3 — TLE data
_No screenshot needed — this part is just adding hardcoded TLE constants._

### Part 4 — Propagate orbit
_No screenshot needed — no visual change yet._

### Part 5 — Orbit path
- [ ] `part-5-orbit-ring.png` — Full cyan glowing orbit loop around the globe.

### Part 6 — 3D model from Cesium ion
Walkthrough screenshots of the ion UI:
- [ ] `part-6-ion-my-assets.png` — `ion.cesium.com` → **My Assets** page.
- [ ] `part-6-ion-add-data.png` — Drag-and-drop upload (or the **Add data** → **3D Model** dialog).
- [ ] `part-6-ion-asset-detail.png` — Asset detail page. Highlight / circle the **Asset ID** number.
- [ ] `part-6-model-on-orbit.png` — Browser view with the ISS 3D model visible on the orbit ring.

### Part 7 — Clock + camera
- [ ] `part-7-tracked.png` — Camera positioned near the ISS, animation widget + timeline visible at the bottom of the viewer.

### Part 8 — Live info box
- [ ] `part-8-overlay.png` — Final app with Cesium's default info box (top-right) showing live lat/lon/alt/speed.

## Conventions
- PNG, ~1600 px wide.
- Use the browser at ~1440×900 for consistency.
- Annotations (arrows / boxes) are welcome — keep them red or yellow for contrast.
